# Try to find the atjit library, headers and compiler plugin.
#  ATJit_INCLUDE_DIRS - the atjit include directory
#  ATJit_LIBRARY_DIRS - library directory needed to use atjit
#  ATJit_LIBRARY      - library needed to use atjit 
#  ATJit_PLUGIN       - compiler plugin

set(ATJit_INCLUDE_DIRS "@CMAKE_INSTALL_PREFIX@/include")
set(ATJit_LIBRARY_DIRS "@CMAKE_INSTALL_PREFIX@/lib")
set(ATJit_LIBRARY "ATJitRuntime")
set(ATJit_PLUGIN "@CMAKE_INSTALL_PREFIX@/lib/EasyJitPass@CMAKE_SHARED_LIBRARY_SUFFIX@")
